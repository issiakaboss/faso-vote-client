name: faso_vote_client
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  get: ^4.7.2
  google_fonts: ^6.2.1
  get_storage: ^2.1.1
  intl: ^0.20.2
  pinput: ^5.0.1
  diacritic: ^0.1.6
  pusher_client_socket: ^0.0.2+7
  laravel_echo_null: ^0.0.8+8
  super_tooltip: ^2.0.9
  clipboard: ^0.1.3
  flutter_dotenv: ^5.2.1
  file_picker: ^10.1.9
  flutter_image_compress: ^2.4.0
  intl_phone_field: ^3.2.0
  google_sign_in: ^6.2.2
  google_sign_in_web: 0.12.4+4
  quickalert: ^1.1.0
  share_plus: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - images/
    - .env
    - .env.production
